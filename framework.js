/*var framework = function(){
	return{
		create: function(element){
			var elo = document.createElement(element);
			return elo;
		},
		append: function(par, element){
			par.appendChild(element);
		},
		tag: function(element){
			var chuj = document.getElementsByTagName(element);
			return chuj;
		},
		attr: function(object, attributes){
			for (var property in attributes) {
				object[property] = attributes[property];
			};
		}
	};
}();

var addNewItemUpcoming = function(){
			var newItemDiv = document.createElement("li");
			listUpcoming.appendChild(newItemDiv);
			newItemDiv.innerHTML = newItemInput.value;
		}; */
